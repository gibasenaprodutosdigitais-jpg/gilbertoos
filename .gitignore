# Dependências (reinstaláveis via npm install)
node_modules/
scripts/node_modules/
scripts/.node-runtime/

# Segredos — NUNCA subir
.env
.env.*
*.key

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Config local do Claude Code (específico de cada aparelho)
.claude/settings.local.json

# Temporários de render
scripts/*.log
